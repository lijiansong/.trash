int main(int argc,char *argv[])
{
	freopen("in.txt","r",stdin);
	freopen("out.txt","w",stdout);
	yylex();
	printf("lines: %d\nwords: %d\nchars: %d\n",lines,words,chars);
	fclose(stdin);
	fclose(stdout);
	return 0;
}
int yywrap(void)
{
return 1;
}
favourite
for what you have do?
colourful
colour
